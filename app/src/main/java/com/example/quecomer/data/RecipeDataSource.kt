package com.example.quecomer.data

import javax.inject.Inject
/*
class RecetaDataSource @Inject constructor(private val recetaService: RecetaService) :
    com.example.quecomer.data.BaseDataSource() {

    suspend fun addUser(user: User) = getResult { recetaService.postRecetas("aca la receta") }
}{
}*/